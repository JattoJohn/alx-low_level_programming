#include <stdio.h>

/**
 * main - "prints the phrase with proper grammar,
 * but the outcome is a piece of art
 * Return: as 0 if existed properly or non-zero otherwise
*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
